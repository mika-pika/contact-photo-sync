# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'Contact Photo Sync' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Contact Photo Sync

  target 'Contact Photo SyncTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Contact Photo SyncUITests' do
    # Pods for testing
  end

end
